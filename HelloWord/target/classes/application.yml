spring:
  application:
    name: HelloWord
  ai:
    openai:
      api-key: ${SPRING_AI_KEY}
      base-url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
      chat:
        options:
          model: qwen3.6-plus